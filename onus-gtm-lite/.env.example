# Required for generation (default provider)
OPENAI_API_KEY=sk-...

# Optional overrides
OPENAI_MODEL=gpt-4o-mini
AI_PROVIDER=openai

# Optional: Lovable gateway (set AI_PROVIDER=lovable)
# LOVABLE_API_KEY=
# LOVABLE_MODEL=google/gemini-2.5-flash

# Local data directory (defaults to ./data)
# DATA_DIR=./data
